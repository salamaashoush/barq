{
  "name": "barq-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Barq - Lightning fast JSX renderer with fine-grained reactivity via signals",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd packages/kitchen-sink dev",
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test",
    "lint": "oxlint --type-aware packages/core packages/server packages/start packages/router packages/extra packages/testing",
    "lint:fix": "oxlint --type-aware --fix packages/core packages/server packages/start packages/router packages/extra packages/testing",
    "format": "oxfmt packages/core packages/server packages/start packages/router packages/extra packages/testing",
    "format:check": "oxfmt --check packages/core packages/server packages/start packages/router packages/extra packages/testing",
    "ci": "oxlint --type-aware --deny-warnings packages/core packages/server packages/start packages/router packages/extra packages/testing && oxfmt --check packages/core packages/server packages/start packages/router packages/extra packages/testing",
    "benchmark": "bun run --cwd packages/benchmark start"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "oxfmt": "^0.64.0",
    "oxlint": "^1.79.0",
    "oxlint-tsgolint": "^7.0.2001",
    "tsdown": "^0.22.14",
    "typescript": "^7.0.2"
  }
}
